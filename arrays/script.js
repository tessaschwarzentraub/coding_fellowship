let groceries = [                           //"let groceries is the variable", the [] is the array
    "avocados", 
    "coconut milk", 
    "lentils",                              //eat different idem is a "memory space"
    "onion", 
    "tomatoes"
];                                          //can also list memory spaces vertically not horizontally, just depends on writer

console.log(groceries[0]);

groceries.push("kiwis");                    //push puts a new item at the end of the list in an array  

console.log(groceries);                     //regular console.log() shows the entire array

